import { Widget } from './widget';
import type { WidgetEventMap } from './widgetEvents';
export declare class ButtonWidget extends Widget<HTMLButtonElement> {
    constructor();
    protected destructor(): void;
    setEnabled(enabled: boolean): void;
    addListener<K extends keyof WidgetEventMap>(type: K, listener: (ev: WidgetEventMap[K], current: this) => unknown): void;
}
